import { VERSION_STAGE_LABELS } from '@/lib/agent-mission-control/labels'

/** Canonical display labels for version stages — never render the raw enum. */
export const versionStageLabels = VERSION_STAGE_LABELS
